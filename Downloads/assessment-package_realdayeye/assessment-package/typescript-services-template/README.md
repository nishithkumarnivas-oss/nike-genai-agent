# TypeScript Services Template

This folder is a minimal TypeScript template included so `npm run build` has input files.

How to build locally:

```bash
cd assessment-package/typescript-services-template
npm install
npm run build
```

It produces compiled output in `dist/`.
